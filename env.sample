GCP_PROJECT_ID=

# Supabase Configuration
# Local: Put actual secret values here
# Cloud Run: These are injected via secretKeyRef
SUPABASE_URL=
SUPABASE_SECRET_KEY_NAME=
SUPABASE_PUBLISHABLE_KEY_NAME=

# JWT Signing Keys
# Local: Put actual JWT secret here
# Cloud Run: Injected via secretKeyRef
SUPABASE_JWT_SECRET_NAME=   

# Google OAuth Configuration
# Local: Put actual OAuth credentials here
# Cloud Run: Injected via secretKeyRef
GOOGLE_CLIENT_ID_NAME=
GOOGLE_CLIENT_SECRET_NAME=
GOOGLE_REDIRECT_URI=

# Security Keys
# Local: Put actual encryption key and session secret here
# Cloud Run: Injected via secretKeyRef
SESSION_SECRET_KEY_NAME=
ENCRYPTION_KEY_NAME=

# Environment
ENVIRONMENT=development