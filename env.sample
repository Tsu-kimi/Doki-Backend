GCP_PROJECT_ID=your-gcp-project-id

# Supabase Configuration (using new publishable/secret keys)
SUPABASE_URL=
SUPABASE_SECRET_KEY=your-supabase-secret-key-sb_secret_xxx
SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key-sb_publishable_xxx

# JWT Signing Keys (get from Supabase > Project Settings > API > JWT Settings)
SUPABASE_JWT_SECRET=your-jwt-secret-for-token-validation

# Google OAuth Configuration
GOOGLE_CLIENT_ID_NAME=
GOOGLE_CLIENT_SECRET_NAME=
GOOGLE_REDIRECT_URI=

# Security Keys
SESSION_SECRET_KEY=generate-a-random-secret-key-for-production
ENCRYPTION_KEY=generate-using-python-cryptography-fernet

# Environment
ENVIRONMENT=development

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
